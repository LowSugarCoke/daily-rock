# Cloudflare Workers with Rust and Next.js Frontend

We have decided to build the Daily Rock backend as a Cloudflare Worker using Rust (`axum` and the `worker` crate), and the frontend using Next.js. This architecture leverages Rust's safety and performance at the edge, while using Next.js for a modern, responsive user interface.
